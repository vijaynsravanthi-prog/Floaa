{
  "name": "floaa-api",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Cloudflare Worker for the FLOAA API.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --config ./wrangler.toml",
    "tail": "wrangler tail --config ./wrangler.toml",
    "logs": "wrangler tail --config ./wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.20.3"
  }
}
